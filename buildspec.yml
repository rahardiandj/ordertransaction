version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
      - ls -a customer/target/customer-0.0.1-SNAPSHOT.jar
artifacs:
  files:
    - customer/target/customer-0.0.1-SNAPSHOT.jar
